package synthetic_diamonds.client;

import net.fabricmc.api.ClientModInitializer;

/** The client entrypoint. Every hook this mod adds is server-side (docs/spec/04-architecture.md); nothing registers here yet. */
public final class SyntheticDiamondsClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
    }
}
