Create: Synthetic Diamonds is MIT-licensed (LICENSE).

It runs on and is built against:
- Create Fly (ZurrTum/Create-Fly), CC0-1.0, mod id `create`: the mechanical press this mod's
  recipes run in.
- Create (Creators of Create), MIT: the upstream Create Fly ports.
- Fabric Loader and Fabric API (FabricMC), Apache-2.0.
- Minecraft (Mojang): the mod's recipes produce the vanilla `minecraft:diamond` item; no Mojang
  asset (texture, model or sound) is copied. This mod adds zero mixin targets
  (docs/spec/decisions/DEC-004-toolchain.md).
