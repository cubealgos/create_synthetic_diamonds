---
title: "create_synthetic_diamonds DEC-002 — create_synthetic_diamonds, mod id synthetic_diamonds, display Create: Synthetic Diamonds"
type: "spec"
category: "create_synthetic_diamonds"
---

# `DEC-002` — create_synthetic_diamonds, mod id synthetic_diamonds, display Create: Synthetic Diamonds

**Status:** decided by Kevin, 2026-09-20.

Follows the folder-is-the-project-name, mod-id-drops-the-`create_`-prefix convention the three
siblings already set. The Modrinth listing title departs from that pattern in one respect: where
`create_brass_compass`, `create_metered_motor`, and `create_villager_customers` all display as
"Create Fly: `<Name>`" — naming the specific Create fork/edition they build on — this mod's listing
is titled **"Create: Synthetic Diamonds"** instead, naming the base mod family rather than the
specific fork. This is Kevin's own choice for this project, recorded here plainly as a deliberate
divergence from the siblings' own established pattern, not a mistake to correct and not a case that
needs further defending against any broader naming convention.

No domain registered, no six-TLD availability check run: there is no standalone web presence at
1.0, exactly as none of the three siblings have one. Revisit trigger: a website for the mod, as for
the three siblings.
